{
    "name": "gearchy-project",
    "version": "0.2.0",
    "private": true,
    "scripts": {
        "build:app": "turbo run build --filter=!site",
        "build:site": "turbo run build --filter=!app",
        "pretty": "prettier --write \"./**/*.{js,ts}\"",
        "build": "pnpm -F './packages/**' build",
        "release": "bumpp package.json app/package.json packages/*/package.json",
        "preview": "vercel dev"
    },
    "devDependencies": {
        "bumpp": "^8.2.1",
        "prettier": "^2.7.1",
        "turbo": "^1.4.6",
        "vercel": "^28.2.5"
    },
    "dependencies": {
        "http-server": "^14.1.1"
    }
}