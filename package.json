{
    "name": "@devidw/gearchy-monorepo",
    "private": true,
    "scripts": {
        "build": "turbo run build --filter=!site",
        "preview": "vercel dev",
        "pretty": "prettier --write \"./**/*.{js,ts}\""
    },
    "devDependencies": {
        "prettier": "^2.5.1",
        "turbo": "^1.4.6",
        "vercel": "^28.2.5"
    }
}